import 'animal.dart';

//abstract class Cattle extends Animal {
  // bool _canMilk = false;

  // Cattle({required bool canmilk}) : super();

  // bool get canMilk => _canMilk;
  // set canMilk(bool canMilk) => _canMilk = canMilk;

  // @override
  // double addCost(double amount) => currentCost = amount;
//}
